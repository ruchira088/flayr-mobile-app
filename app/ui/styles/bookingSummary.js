import React from "react"
import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        padding: 20
    },
    lineItem: {
        flexDirection: "row",
        alignItems: "center",
    },
    lineItemLabel: {
        fontSize: 18,
        fontWeight: "bold",
        marginRight: 10
    },
    lineItemValue: {
    }
})
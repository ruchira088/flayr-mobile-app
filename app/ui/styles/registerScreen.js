import React from "react"
import { StyleSheet } from "react-native"

export default StyleSheet.create({
    title: {
        fontSize: 60,
        fontWeight: "bold",
        textAlign: "center",
        margin: 30
    },
    description: {
        fontSize: 20,
        padding: 10
    }
})